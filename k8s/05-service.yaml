apiVersion: v1
kind: Service
metadata:
  name: java-api-service
  namespace: java-api-ns-yanushka
  labels:
    app: java-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: java-api